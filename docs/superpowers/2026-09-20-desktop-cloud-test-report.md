# 桌面与云端助手测试记录

日期：2026-09-20。环境：macOS 26.5.1 / Apple Silicon，Python 3.12.13，Node.js 22。

## 已通过

| 检查 | 结果 |
| --- | --- |
| `python -m unittest discover -s tests/desktop -v` | 26 / 26 |
| `cd cloud && npm test` | 35 / 35，使用真实 Miniflare D1 |
| `cd cloud && npm run typecheck` | 通过 |
| `node --check desktop/web/app.js` | 通过 |
| PyInstaller macOS ARM64 构建 | 通过 |
| `tests/desktop/packaged_smoke.py` | 程序启动、静态资源、认证 API、正常退出、打包内置代理及 CA 生成通过 |
| 浏览器流程 | 邀请码、恢复码展示、获取测试登录态、授权上传、验证、绑定、代理清理、执行、暂停/恢复、设置、解绑通过 |
| 浏览器异常流程 | 验证过期、云端激活失败均提示错误，不显示绑定成功 |
| 布局与资源 | 桌面及 390px 视口检查；无页面横向溢出，手机配对二维码正常显示 |
| 凭证泄露检查 | 对 75 个候选源码文件匹配本机真实凭证，0 处命中；未输出凭证值 |

真实代理测试独立生成临时 CA，检查配对前拒绝采集、配对后接受指定手机、证书不含私钥、关闭采集后进程仍保持运行、停止后子进程退出。

浏览器使用测试后端和测试积分，未提交个人登录态，未对真实领克账号签到或分享。

## 测试中修复

1. 浏览器刷新请求方式错误，已统一为本地 API 要求的 POST。
2. 最后一次分享尝试崩溃后，结果不再停留为进行中，改为 `unknown / RESULT_UNKNOWN`，不会自动重放。
3. PyInstaller 入口与包资源目录不同，已使用实际包目录定位界面和服务配置。
4. HTTP 服务启动会进行反向 DNS 查询，可能长时间卡住；已移除此无用查询，并增加回归测试。
5. 过期绑定预览恢复到重新验证步骤，激活失败保留预览且不标记成功。

## 云端部署

- 独立 Worker：`lynkco-helper-desktop`。
- 独立 D1：`lynkco-helper-desktop`，初始迁移已执行成功。
- 已部署定时触发器，每分钟检查到期任务。
- 仅上传应用级签名配置，管理主密钥保存在本机系统钥匙串；没有上传原有个人 token。
- 默认服务地址：`https://lynkco-helper-desktop.728729140.workers.dev`。

当前网络访问默认域名超时；本地 DNS 返回的地址与可信 HTTPS DNS 查询结果不同，指定可信解析地址连接仍被重置。因此不能将部署成功等同于当前用户网络已可用。已有域名 `ltools.asia` 的新子域名绑定等待用户选择。

账号订阅查询受现有 OAuth 权限限制，未完成账户套餐核验；没有调用升级或购买接口。线上请求/定时任务 CPU、子请求额度尚未测量，不能用本地测试耗时替代免费额度验收。

## 未完成验收

- 真实 iPhone、安卓登录态获取、云端验证、签到及分享兼容性。
- Windows 和 macOS Intel 实机发行包测试，系统签名/公证分发体验。
- 云端可访问域名确认、实际免费 CPU/子请求/数据库消耗测量、20 用户排队情况。
- 7 天稳定运行观察。
- 桌面首版未提供 Bark 配置；原命令行 Bark 保持原样。

结论：本地实现与模拟上游的功能测试通过，macOS ARM64 试用包可运行；尚未满足面向所有用户正式发布的全部验收条件。

## iPhone 联调准备补充

用户授权使用 `lynkco.ltools.asia` 后，已绑定独立自定义域名，健康检查返回 `configured: true`，真实管理账号创建成功。旧 `workers.dev` 的网络不可达问题已通过新入口解决；未升级套餐。

本机实际 Wi-Fi 使用非 RFC1918 地址，修复了网络列表仅允许传统私网段的问题，同时保留对停用网卡、VPN 和回环接口的排除。Cloudflare 对 Python 默认客户端标识返回 1010，已改为明确的 `LynkCoHelper/0.1 (Desktop)` 标识，实际请求验证通过。

新增两项回归测试后，桌面 28 / 28 通过，云端原有 35 项未修改。应用重新打包，真实内置代理已启动。恢复码在用户浏览器中展示，管理凭证已存系统钥匙串，应用主密钥按新域名保存同值别名，未轮换加密密钥。

当前停在手机操作阶段：等待 iPhone 扫码配对、安装并完全信任证书、配置 Wi-Fi 手动代理及打开领克 App。尚未取得手机登录态，尚未执行真实账号签到或分享。
